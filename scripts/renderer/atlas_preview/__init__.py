"""Atlas portal renderer — public-clean subset of the Atlas preview renderer.

Builds the scope_data JSON for an Atlas Edit Proposal diff. Consumed by
atlas-portal's `/proposal` page to render the community-facing diff view.
"""

__version__ = "0.1.0"
